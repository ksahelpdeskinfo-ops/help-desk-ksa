import { Metadata } from "next";

export const metadata: Metadata = {
  title: "File Labor Complaint Saudi Arabia: Step-by-Step Guide",
  description: "Official guide on how to file a labor complaint in Saudi Arabia for salary delays, contract violations, and worker rights protection.",
  keywords: ["file labor complaint Saudi Arabia", "worker rights KSA", "salary delay complaint Saudi"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
